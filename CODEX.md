# Codex Instructions

The canonical workspace instructions are in `AGENTS.md`.

Codex agents working in `C:\DevWork\GodotGame` must read and follow
`AGENTS.md`, then apply any narrower project-level `AGENTS.md` inside the
active project folder.

The procedural harness is always-on through `AGENTS.md`. Users do not need to
mention fablize, prometheus, or goal ledger for baseline inspection,
verification, and early-stop rules to apply. Load the local skill at
`.codex-skills/procedural-verification-harness/SKILL.md` for substantial
multi-step, debugging, review, or runtime-rendered artifact work.
